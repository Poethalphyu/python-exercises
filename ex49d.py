def match(word_list,excepinf):
    if word_list:
        word=word_list.pop(0)
        if word[0]==excepting:
            return word
        else:
            return None
        else:
            return None


